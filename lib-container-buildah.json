{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "regexManagers": [
    {
      "fileMatch": [".*\\.sh", ".*\\.bash"],
      "datasourceTemplate": "docker",
      "matchStrings": [
        "\\$\\(buildah from (?<depName>.*?)\\)"
      ]
    }
  ]
}
